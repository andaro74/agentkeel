# Carried into M03

Written at M02's close (PR 3, #13, 2026-09-24). `/open-milestone` reads
this file first. An item dated "before M03 PR 1" is answered before that
PR's first commit; "M03 PR 1" is in it; a later milestone is where the
item's claim is measured, and it waits there. Sources: the M02 cold
reviews (`milestones/M02/rulings/pr1-cold-review.md`, `pr2-cold-review.md`,
`pr3-cold-review.md`), the seat rulings, the PR bodies' Unsure lists, and
`milestones/M02/open.md` rows that M02 did not close.

| # | Item | Seat | When |
|---|---|---|---|
| 1 | **SPEC/02 §2's closed list, one amendment** (a SPEC/00 §5 amendment by §2's own words): a `relaxes:` entry changed, a bar deleted, manifest budgets raised (PR 2 threshold F2, F3, F4); a guardrail set to `null` (PR 2 cold F5b); the second key "names" a path by substring, to become a `keys:` field or a fixed body line (PR 2 security F4); how a ruling names a deletion, since `validate` now matches a glob against paths deleted in history, history-wide, and a rename is not a deletion (PR 3 `pr3.md` finding 3, cold N4, security N15). The Threshold Owner proposes; lands before the regression bar. | Product, with the Threshold Owner | M03 PR 1 |
| 2 | **SPEC/01 §9's ceiling bullet:** the agent boundary refused a call once, unplanned (run 35817173042, fifteen `AccessDeniedException`s on `dynamodb:Scan`), recorded as "observed once, unplanned"; a seeded case where a construct grant outside the ceiling is refused at the call (PR 3 platform F2). | Product (the bullet); Security (the case) | M03 PR 1; M05 |
| 3 | **The instruments' loose ends:** attempt 2's CI line (`ci_red_lines`) read by `build.check_from_bypass`, not only recorded (PR 2 cold F3, PR 3 cold N3); `gate.rule` prints `where` from `golden_kinds_at` (PR 2 cold N3); `lines_naming` strips before removing the timestamp (PR 2 cold N7); `judge`'s default is claim 1 only and a direct caller would skip claim 2 (PR 3 cold N2). | Engineering | M03 PR 1 |
| 4 | **M03's bar shape:** `bars` reads one level deep; the regression bar lands as `delta_max` and R10's N as `up` (PR 2 threshold N4, N5). | Threshold Owner, Engineering | M03 PR 1 |
| 5 | **`g-021` and what an absence trap cites:** no agent passes it under refagent's tool contract (PR 2 data F1; PR 12 Unsure G); how an absence trap cites is the Data Owner's and the not-found candidates the Tool Owner's; scoring compares the cited row to the golden's (PR 2 data N2). | Data Owner, Tool Owner, Engineering | M03 PR 1 |
| 6 | CLAUDE.md says `validate` checks golden/retrieval overlap; no such check exists until the corpus lands (PR 2 data N1). | Product | M03, with `data/corpus/` |
| 7 | **The Threshold Owner's re-rules at M03:** the cap of 150,000 against 54,009 spent, re-ruled when refagent grows a knowledge base (`pr2-threshold-owner.md` §3, §8); the control still answers `g-012` every run, a fixed cost while ADR-0002 holds (PR 2 cold N6, data N3). | Threshold Owner | M03 |
| 8 | `scripts/read_back_grants.py` probes no DynamoDB action: `("dynamodb:Scan", "allowed")` and `("dynamodb:PutItem", "implicitDeny")`, run once with admin, the rows in `infra/bootstrap/README.md` (PR 3 platform N1). | Security | M03 PR 1 |
| 9 | **M02's video**, recorded on `main` at tag `m02`, ≤ 5 minutes and ≤ 40 MiB, committed as an LFS object with its row in `docs/video/README.md` (ADR-0005 amendment 1). And M01's recording is 6:09, over the five-minute ceiling: rule whether the ceiling or the recording stands, and say so in the row. | Product | M03 PR 1 |
| 10 | **The human's housekeeping after the merge:** the seed PRs 14 to 18 closed unmerged with their branches kept (SPEC/02 §5.1); the two top-level `doors:` blocks in `milestones/M02/runs/f2_2_three_doors.yaml` collapsed or left as the record of the fill (PR 13 Unsure A; PR 3 cold F1) if not done before the merge; the image `822fe2b5` in ECR deleted or left (PR 12 Unsure F). | Product, Security (the human) | before M03 PR 1 |
| 11 | `milestones/M02/open.md` rows 12 and 13 (the instruments behind F1.1 and F1.3; Engineering's smaller items) were dated M02 PR 2 and are not verified item by item at this close: check each against `pr2-engineering.md` and `pr2-cold-review.md`, and re-date what is not done. | Engineering | before M03 PR 1 |
| 12 | **M04's:** `pinned_roles` carry region and version and the T2 limit (M02 open.md row 19); `check_model_access` (`pr2-threshold-owner.md` §9, not needed at M02 PR 3 since the runtime answered); the stub's model fields moving with the swap (§7). | Threshold Owner | M04 PR 1 |
| 13 | **M06's and M07's:** ceilings computed from the org graph, not bounded (PR 2 tool F3), with the second real agent; the rest of M02 open.md row 20 with `ratings-helper`'s code (`pr2-tool-owner.md`). | Tool Owner | M06; M07 |
| 14 | **M05's, from M02:** the bot exemption reads `%an`, an author name anyone sets (PR 2 cold F4); the reader is the PR's own code and anyone with write can edit a workflow and its hash in one PR (`evals.yml` header, PR 3 security N13); the eval role's Deny has no recorded refusal (item 18's probe; PR 3 platform N3); hashing what a workflow runs (M02 open.md row 7, second half); M02 open.md rows 11, 15, 23 and 24 as dated; the GitHub OIDC provider's origin, landing-zone work (PR 3 platform N4). | Security | M05 open |
| 15 | Read at M03 PR 1 before anything else: the M02 ledger row and the envelope `8033c2a`, not this file's prose; the three envelopes `12b4646`, `47258f2`, `6daf6c4` rule RED under `CLAIM_2_CHECKS` and nothing cites them; from M03 every agent envelope carries `F2_1` and `F2_2` or is RED. | Product, Engineering | at M03 open |
