# The three doors (SPEC/00 §8 M02, F2.2; SPEC/02 §5.1). Filled by the
# human after M02 PR 2 merges; PR 3's CI run looks each up
# (`scripts/observe_pr.py`) and that lookup writes `checks.F2_2`. M06's
# Act 2 is recorded against these PRs (SPEC/00 §10.2).
doors:
  - door: 1
    what: a relaxation without two keys, blocked by the gate
    pr: null  # seed S1's PR, from f2_1_seed_prs.yaml
    expected: closed unmerged; check two-key failed on its head sha, naming thresholds.yaml
  - door: 2
    what: the same kind of change with two keys, merged
    pr: null  # M02 PR 2
    expected: merged; check two-key succeeded on its head sha; the merge commit carries two ruling files with distinct seats and pr equal to this number, both covering evals/goldens/v1/g-012.yaml
  - door: 3
    what: the owner attempting bypass, blocked by two gates
    pr: null  # seed S1's PR again; the attempt is f2_1_bypass.yaml attempt 1, the second gate attempt 2
    expected: the merge refused to the owner with bypass_actors [] at that time; validate RED on the ruleset that would have allowed it
observed: null
